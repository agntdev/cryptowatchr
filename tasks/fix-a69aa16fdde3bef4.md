# fix-a69aa16fdde3bef4 — Fix: Failing dialog spec: turn off summary from settings

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 CWTR

**Severity:** high

The Tests-gate replay harness reported spec "turn off summary from settings" as failing. 132 spec(s) failed: [selecting a timezone shows defaults and confirmation confirming onboarding shows the main menu skipping onboarding shows the main menu typing a custom timezone during onboarding skip auto-detects timezone from language code (ru -> UTC+3) skip auto-detects UTC+0 as fallback with unknown language skip auto-detects from zh language code to UTC+8 skip auto-detects UTC+0 when language_code is absent setting custom quiet hours during onboarding keeping default quiet hours by skipping setup invalid time input shows error during quiet hours setup Add Coin menu button shows coin selection keyboard Add BTC to watchlist from coin selection Add ETH to watchlist from coin selection Add TON to watchlist from coin selection Custom ticker button shows text entry prompt Add custom ticker SOL to watchlist Adding duplicate BTC shows already in watchlist Unknown ticker shows suggestions and returns to coin selection Empty ticker shows validation error Back to menu from watchlist coin selection /list when watchlist is empty shows empty message /list after adding a coin shows watchlist with Remove button Remove coin from watchlist via inline button menu:watchlist button shows empty watchlist menu:watchlist shows coins with Remove buttons Remove one of multiple coins via /list shows remaining coins Remove coin via My Watchlist menu leads to empty state Remove all coins one by one leaves empty watchlist create a threshold alert for BTC below $60,000 percent alert shows coming soon notification back navigation from alert flow to main menu invalid price shows validation error cancel from price prompt returns to main menu entering /start during alert flow resets state create a percent alert for BTC at 5% in 1 hour create a percent alert for any coin in my list create a percent alert with custom ticker invalid percentage shows validation error invalid timeframe shows validation error back navigation from percent coin selection to alert type cancel from percent flow returns to main menu my alerts menu shows empty state /alerts command shows empty state view threshold alert in my alerts list view percent alert in my alerts list back to menu from alerts list /alerts command shows existing threshold alert /start resets alert manage session /price with empty watchlist shows empty message /price with unknown ticker shows error menu:price with empty watchlist shows empty message /price with a coin added to watchlist attempts fetch menu:price with a coin added to watchlist attempts fetch /summary command shows off state settings menu shows morning summary off turn on summary from settings and set time settings menu shows summary on after enabling turn off summary from settings turn on summary from /summary command /summary shows on state after enabling change summary time /start clears summary session invalid time format shows error and allows retry back from time prompt returns to settings menu:back clears summary session and returns to main menu threshold alert notification uses required format with action buttons view alert button opens alert details pause alert button suppresses further notifications /price with unknown ticker shows friendly message /price on empty watchlist shows empty message /price with empty ticker arg treated as no-arg morning summary persists across multiple settings opens morning summary persists after /start and re-onboarding timezone is persisted across onboarding confirmation custom quiet hours are persisted during onboarding settings show summary off after onboarding with timezone only disable summary persists across settings opens watchlist persists after navigating away and back watchlist survives /start restart alert rule persists after navigating away and back alert rule survives /start restart watchlist and alerts both survive /start price command with unknown ticker shows error price command without ticker shows empty watchlist price check menu button shows empty watchlist /price with unknown ticker returns error /price with no args and empty watchlist shows empty message Price Check menu button with empty watchlist shows empty message /price with a known ticker fetches price /price with no args and coins in watchlist fetches prices Price Check menu button with coins in watchlist fetches prices /price with no args and empty watchlist shows empty message Price Check menu button with empty watchlist shows empty message /price with valid watchlist shows prices without extra warnings /start shows the CryptoWatchr main menu main menu routes to a top-level feature /help shows the command list with the main menu settings menu shows timezone quiet-hours and summary sections set timezone from settings persists across settings opens set custom timezone via free-text input from settings set quiet hours from settings clear quiet hours from settings morning summary via settings returns to enhanced settings view /admin_stats displays admin statistics for owner /admin_stats is denied for non-owner main menu includes My Alerts button after onboarding /alerts command shows My Alerts button in main menu fix-17380dcad8dae381: menu:price with empty watchlist shows empty message fix-17380dcad8dae381: menu:price with a coin added to watchlist fetches prices threshold alert custom ticker rejects unknown coin threshold alert custom ticker accepts known coin and proceeds to direction /price with valid watchlist shows prices without extra warnings /price with single ticker argument shows coin price /price with unknown ticker shows error message /price with valid watchlist shows prices without extra warnings main menu includes My Alerts button My Alerts button navigates to empty alerts view threshold alert notification includes absolute dollar change /admin_stats displays admin statistics for owner /admin_stats is denied for non-owner /admin_stats denied for non-owner when TELEGRAM_OWNER_ID is set /admin_stats returns stats when called by the bot owner /price with valid watchlist sends single combined message without separate warning menu:price with empty watchlist shows empty message menu:price with a coin added to watchlist attempts fetch menu:price with empty watchlist shows empty message menu:price with a coin in watchlist fetches prices /list clears summary session — stale summary state does not capture free text menu:watchlist clears summary session — stale summary state does not capture free text menu:add clears summary session — stale summary state does not capture free text /price with valid watchlist shows prices without extra warnings]

## Dialog tests

This is a FIX task: the behavior it repairs is already covered by an existing spec under `tests/specs/`. Fix the code to make that existing spec pass — do NOT author a new `tests/specs/fix-a69aa16fdde3bef4.json` (a duplicate spec for the same behavior makes the tests-gate count it twice and it can never go green). Add a new spec file ONLY if you are introducing genuinely new user-facing behavior that no existing spec covers; if so, name it `tests/specs/fix-a69aa16fdde3bef4.json` (and any new command `tests/commands/fix-a69aa16fdde3bef4.json`).


## Handler module

This is a FIX task. Find the EXISTING handler under `src/handlers/` that implements the affected command/behavior and EDIT it in place. Do NOT create a new `src/handlers/fix-a69aa16fdde3bef4.ts` — a second `Composer` binding the same command conflicts with the original and breaks the bot. Create a new handler file ONLY if the affected command does not exist anywhere yet (then name it `src/handlers/fix-a69aa16fdde3bef4.ts` and default-export a grammY `Composer`; `buildBot()` auto-loads it). NEVER edit `src/bot.ts`; the global error boundary + unknown-command fallback already live in `buildBot()`.


## Implementation contract

Ship a COMPLETE, working implementation — not a stub. A task is INCOMPLETE (and will be rejected) even if it compiles and the dialog tests pass when it does any of these:
- **Stubbed code:** empty bodies, `TODO`/`FIXME`, commented-out logic, or `throw new Error("not implemented")`.
- **Fabricated data:** `Math.random()`, hardcoded sample arrays, or canned responses standing in for real computed or fetched values.
- **No in-memory data store:** a `Map`/array/module-level variable used as a database is a defect. Anything that must survive a restart (records, subscriptions, balances, schedules, settings) MUST use the toolkit's persistent storage (Redis-backed), not process memory. (The toolkit's auto-selected session storage is only for ephemeral conversation state.)
- **Broken integrations:** call external APIs against their real contract — correct endpoints, ids and params (e.g. a coin *id* like `the-open-network`, not a ticker like `TON`) — with credentials read from env. Do not invent endpoints or fake responses.
- **Dead code:** the feature's command/handler must be registered via its default-exported `Composer` in `src/handlers/<slug>.ts` (auto-loaded) and reachable from the bot's command surface.
If the spec is genuinely under-specified, implement the smallest REAL slice you can verify and note the gap — never fake behavior to make the PR look complete.
